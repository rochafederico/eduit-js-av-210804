// var variable = "Algo";

var fede = 'fede'

if (fede === 'fede') {
    // contexto, closed
    // debugger;
    var apellido = 'rocha';
}

let varibleQueSePuedeModificar = "";
varibleQueSePuedeModificar = 222;

const varibleQueNOSePuedeModificar = "asdasd";
// varibleQueNOSePuedeModificar = 12312; // NO SE PUEDE

const miObjeto = {
    direccion: {
        calle: 'Calle',
        nro: 123
    }
}

miObjeto.direccion = {};


if (fede === 'fede') {
    // contexto, closed
    const apellidoDos = 'Williams';
}
